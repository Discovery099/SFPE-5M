# Default search space for walk-forward optimization (Phase 6).
# v1: stub only — populated in v5 when WF is implemented.

engines:
  vol_budget:
    target_bars_per_session: [4, 6, 8, 10, 12]
    variance_lookback_sessions: [10, 20, 40]
    sigma_mult: [0.7, 1.0, 1.3]
  dollar_imbalance:
    target_bars_per_session: [6, 8, 10, 12]
    imbalance_window: [30, 50, 80]
    theta_mult: [0.8, 1.0, 1.2]
